<% @projects.each do |project| %>
  <div class="col-md-3">
    <div class="message">
      <div class="message-title"><%= link_to project.title, project_path(project) %></div>
      <p class='message-desc'><%= project.description %></p>
    </div>
  </div>
<% end %>
